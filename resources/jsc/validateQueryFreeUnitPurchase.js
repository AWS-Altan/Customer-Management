var totalAmt = context.getVariable("rspQFU.totalAmt");

/*if(totalAmt !== null && totalAmt !== "" && totalAmt !== undefined && totalAmt !== 0){
    	context.setVariable("validateTotal", "true");
   }else{
    	context.setVariable("validateTotal", "false");
   } */
   
   context.setVariable("validateTotal", "true");